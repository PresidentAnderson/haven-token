{
  "name": "haven-token-contracts",
  "version": "1.0.0",
  "description": "HAVEN Token Smart Contracts",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "test:testnet": "hardhat test --network baseSepolia",
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network base",
    "verify": "hardhat run scripts/verify.ts",
    "coverage": "hardhat coverage",
    "lint": "solhint 'contracts/**/*.sol'",
    "lint:fix": "solhint 'contracts/**/*.sol' --fix"
  },
  "keywords": ["haven", "token", "blockchain", "base"],
  "author": "PVT",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "@types/node": "^20.10.0",
    "hardhat": "^2.19.2",
    "solhint": "^4.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0"
  }
}
